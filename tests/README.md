# Tests

Repository-level validation tests live in `scripts/validate_repo.py` during Milestone 0. Dedicated tests will be introduced when paper structure, citations, figures, or build behavior are added.
